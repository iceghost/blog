const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			body: ['Athiti', 'sans-serif'],
			disp: ['Calistoga', 'serif']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
